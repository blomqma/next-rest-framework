{
  "name": "example",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "prebuild": "cd ../.. && pnpm build && cd apps/example",
    "build": "pnpm prebuild && next build",
    "start": "next start",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "next-rest-framework": "0.5.0"
  }
}
